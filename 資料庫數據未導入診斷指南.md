# 資料庫數據未導入診斷指南

## 🔍 問題描述

**症狀**：資料庫中有 6770.TW 的數據，但程式啟動後沒有自動導入

**資料庫數據範例**：
```
1034    6770.TW    2025-11-25T09:45:37.983+08:00    33.65    82237    33.6    33.65
1059    6770.TW    2025-11-25T09:46:49.025+08:00    33.55    82585    33.55   33.6
1084    6770.TW    2025-11-25T09:47:48.620+08:00    33.55    82699    33.55   33.6
1109    6770.TW    2025-11-25T09:48:48.619+08:00    33.45    83187    33.45   33.5
```

## 🛠️ 診斷步驟

### 步驟 1：運行測試工具

執行以下批次檔來測試資料庫載入：

```batch
雙擊：測試資料庫載入.bat
```

**預期輸出**：
```
[步驟 1] 初始化 MarketDataLoader...
✓ 初始化成功

[步驟 2] 載入 6770.TW 今日開盤至今的數據...

========================================
查詢結果
========================================
符號: 6770.TW
數據筆數: XXX

✓ 成功載入 XXX 筆數據

前 10 筆數據：
----------------------------------------
1. 時間: 2025-11-25T09:45:37 | 價格: 33.65 | 成交量: 82237
2. 時間: 2025-11-25T09:46:49 | 價格: 33.55 | 成交量: 82585
...
```

### 步驟 2：檢查程式啟動日誌

重新啟動程式並查看完整日誌，尋找以下關鍵訊息：

#### ✅ **成功的日誌**：
```
[AWT-EventQueue-0] INFO  - MarketDataLoader 初始化成功
[FinMind-Startup] INFO  - ========================================
[FinMind-Startup] INFO  - 嘗試從資料庫載入 6770.TW 的歷史數據
[FinMind-Startup] INFO  - ========================================
[FinMind-Startup] INFO  - ✓ 從資料庫成功載入了 XXX 筆tick數據
[FinMind-Startup] INFO  - 時間範圍: 2025-11-25T09:45:37 ~ 2025-11-25T09:48:48
[FinMind-Startup] INFO  - 價格範圍: 33.45 ~ 33.65
[FinMind-Startup] INFO  - ✓ 已通知 X 個監聽器
[FinMind-Startup] INFO  - ========================================
[FinMind-Startup] INFO  - 資料庫數據載入完成
[FinMind-Startup] INFO  - ========================================
```

#### ⚠️ **失敗的日誌**：

**情況 A：資料庫連接失敗**
```
[AWT-EventQueue-0] WARN - MarketDataLoader 初始化失敗 - 將僅使用 FinMind API
```
**解決**：檢查 MySQL 服務是否啟動

**情況 B：沒有數據**
```
[FinMind-Startup] WARN - ⚠ 資料庫中沒有 6770.TW 的今日數據
[FinMind-Startup] WARN - 請檢查：
[FinMind-Startup] WARN -   1. MarketDataCollector 是否正在運行
[FinMind-Startup] WARN -   2. 資料庫表 ticks 中是否有 6770.TW 的記錄
```
**解決**：確認 MarketDataCollector 正在收集數據

**情況 C：不在交易時間**
```
[FinMind-Startup] DEBUG - 今天是週末，市場未開盤
```
或
```
[FinMind-Startup] DEBUG - 當前時間不在交易時間內（09:00-13:30）
```
**解決**：自動載入只在交易時間內執行，非交易時間請手動點擊「載入歷史數據」按鈕

**情況 D：沒有監聽器**
```
[FinMind-Startup] WARN - ⚠ 沒有監聽器訂閱 6770.TW
```
**解決**：確認已在程式中訂閱該股票

### 步驟 3：檢查自動載入條件

資料庫數據**自動載入**需要滿足以下條件：

```java
if (marketDataLoader != null &&           // ✓ MarketDataLoader 初始化成功
    marketDataLoader.isInitialized() &&   // ✓ 資料庫連接正常
    isMarketOpen()) {                     // ✓ 當前為交易時間（週一~週五 09:00-13:30）

    loadHistoricalDataFromDatabase(symbol);
}
```

#### 檢查清單：

- [ ] **MarketDataLoader 是否初始化成功**
  - 查看日誌：`MarketDataLoader 初始化成功`
  - 如果失敗：檢查 MySQL 服務

- [ ] **是否在交易時間內**
  - 週一~週五：09:00-13:30
  - 非交易時間請使用手動按鈕

- [ ] **是否已訂閱股票**
  - 在程式中輸入 `6770.TW` 並訂閱
  - 查看日誌：`訂閱商品: 6770.TW`

- [ ] **資料庫中是否有今日數據**
  - 使用 SQL 查詢：
    ```sql
    SELECT COUNT(*) FROM ticks
    WHERE symbol='6770.TW'
    AND DATE(ts) = CURDATE();
    ```

### 步驟 4：手動載入測試

如果自動載入失敗，測試手動載入：

1. **啟動程式**
2. **訂閱 6770.TW**
3. **點擊工具列的「📊 載入歷史數據」按鈕**
4. **觀察彈出訊息**

**成功訊息**：
```
成功從資料庫載入 XXX 筆歷史數據！
```

**失敗訊息**：
```
資料庫中沒有找到今日 6770.TW 的數據！
請確認 MarketDataCollector 已經運行並收集了數據。
```

## 🔧 常見問題與解決方案

### 問題 1：MarketDataLoader 初始化失敗

**症狀**：
```
WARN - MarketDataLoader 初始化失敗
```

**可能原因**：
1. MySQL 服務未啟動
2. 資料庫 `market_data` 不存在
3. 連接資訊錯誤

**解決方案**：

#### 檢查 MySQL 服務
```batch
# 方法 1：使用 Windows 服務管理
services.msc → 找到 MySQL → 確認狀態為「執行中」

# 方法 2：命令列測試
mysql -h localhost -u root -p -e "SHOW DATABASES;"
```

#### 創建資料庫（如果不存在）
```sql
CREATE DATABASE IF NOT EXISTS market_data
CHARACTER SET utf8mb4
COLLATE utf8mb4_unicode_ci;
```

#### 檢查連接資訊
預設連接：
- **主機**: localhost
- **端口**: 3306
- **用戶**: root
- **密碼**: （空）
- **資料庫**: market_data

### 問題 2：資料庫有數據但查詢為空

**症狀**：
- 資料庫中有記錄
- 但 `loadTodayMarketOpenToNow()` 返回 0 筆

**可能原因**：
1. 時區問題
2. 時間格式問題
3. 查詢條件不匹配

**診斷**：

#### 檢查數據時間格式
```sql
-- 查看原始數據
SELECT symbol, ts, DATE(ts), TIME(ts)
FROM ticks
WHERE symbol='6770.TW'
LIMIT 5;

-- 確認今日數據
SELECT COUNT(*)
FROM ticks
WHERE symbol='6770.TW'
AND DATE(ts) = CURDATE();

-- 確認開盤時間後的數據
SELECT COUNT(*)
FROM ticks
WHERE symbol='6770.TW'
AND ts >= CONCAT(CURDATE(), ' 09:00:00')
AND ts <= NOW();
```

#### 檢查時區設置
```sql
-- 查看 MySQL 時區
SELECT @@global.time_zone, @@session.time_zone;

-- 應該返回：SYSTEM 或 +08:00
```

### 問題 3：非交易時間無法自動載入

**症狀**：
- 週末或非交易時間啟動程式
- 自動載入不執行

**解決方案**：
這是正常行為。在非交易時間：

1. **使用手動按鈕**
   - 點擊「📊 載入歷史數據」按鈕

2. **暫時禁用時間檢查**（開發用）
   - 修改 `FinMindFeed.java:251`
   - 移除 `&& isMarketOpen()` 條件
   - **注意**：正式環境不建議

### 問題 4：數據導入但圖表未顯示

**症狀**：
- 日誌顯示「✓ 已通知 X 個監聽器」
- 但圖表沒有更新

**可能原因**：
1. 監聽器處理錯誤
2. 圖表組件未刷新
3. 數據轉換問題

**診斷**：

檢查 ChartDock 的 `onTick()` 方法是否正常執行：

```java
// 在 ChartDock.onTick() 添加日誌
logger.debug("收到 Tick: {} @ {} = {}",
    tick.getSymbol(),
    tick.getTimestamp(),
    tick.getPrice());
```

## 📋 完整診斷清單

### 環境檢查
- [ ] MySQL 服務正在運行
- [ ] 資料庫 `market_data` 存在
- [ ] 表 `ticks` 存在且有數據
- [ ] MarketDataCollector 正在運行

### 程式檢查
- [ ] MarketDataLoader 初始化成功
- [ ] 已訂閱目標股票（如 6770.TW）
- [ ] 當前為交易時間（或使用手動按鈕）
- [ ] 日誌中沒有錯誤訊息

### 數據檢查
- [ ] 資料庫中有今日數據
- [ ] 數據時間在 09:00 之後
- [ ] 股票代碼格式正確（6770.TW）
- [ ] 時區設置正確（Asia/Taipei）

## 🚀 快速測試命令

```batch
# 1. 測試資料庫連接
mysql -h localhost -u root -e "USE market_data; SELECT COUNT(*) FROM ticks WHERE symbol='6770.TW';"

# 2. 測試 Java 載入
雙擊：測試資料庫載入.bat

# 3. 查看完整日誌
雙擊：啟動-完整編譯.bat
# 觀察控制台輸出

# 4. 手動載入測試
# 啟動程式 → 訂閱 6770.TW → 點擊「載入歷史數據」按鈕
```

## 📞 進一步協助

如果按照以上步驟仍無法解決，請提供以下資訊：

1. `測試資料庫載入.bat` 的完整輸出
2. 程式啟動時的完整日誌（特別是包含 "MarketDataLoader" 的行）
3. 以下 SQL 查詢結果：
   ```sql
   SELECT COUNT(*) FROM ticks WHERE symbol='6770.TW';
   SELECT COUNT(*) FROM ticks WHERE symbol='6770.TW' AND DATE(ts) = CURDATE();
   SELECT MIN(ts), MAX(ts) FROM ticks WHERE symbol='6770.TW' AND DATE(ts) = CURDATE();
   ```

---

**最後更新**: 2025-11-25
**適用版本**: DreamHouseTrading 0.0.1-SNAPSHOT
