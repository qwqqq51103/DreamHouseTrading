# 指標參數應用功能測試指南

## ✅ 功能已完成

**指標參數設定對話框** 現已完全整合並可實際修改圖表中的指標參數！

---

## 📋 測試步驟

### 1️⃣ 開啟指標設定對話框

**方式一：選單**
- 點擊主選單 → `View` (檢視) → `Indicator Settings` (指標設定)

**方式二：快捷鍵** *(未來可以實作)*
- `Ctrl+I` (建議)

---

### 2️⃣ 載入當前設定

對話框會自動載入圖表中目前的指標配置：

| 指標 | 預設參數 | 預設顏色 |
|------|---------|---------|
| **SMA** | 週期 20 | 藍色 (#007BFF) |
| **EMA** | 週期 20 | 橙色 (#FF8C00) |
| **RSI** | 週期 14 | 金橙色 (#FFA500) |
| **MACD** | 12/26/9 | - |
| **BOLL** | 週期 20, 倍數 2.0 | - |
| **KD** | K:9, D:3 | - |
| **ADX** | 週期 14 | - |
| **CCI** | 週期 14 | - |
| **Williams %R** | 週期 14 | - |

---

### 3️⃣ 修改參數

#### 📊 **疊線指標 (Overlay Indicators)**

1. **SMA (簡單移動平均線)**
   - 調整週期：1~200
   - 點擊顏色按鈕選擇新顏色
   - **測試範例**：週期改為 `10`，顏色改為 `紅色`

2. **EMA (指數移動平均線)**
   - 調整週期：1~200
   - 選擇顏色
   - **測試範例**：週期改為 `50`，顏色改為 `綠色`

3. **BOLL (布林通道)**
   - 調整週期：1~100
   - 調整倍數：0.5~5.0 (步長 0.1)
   - **測試範例**：週期 `30`，倍數 `1.5`

#### 📈 **副圖指標 (Sub Indicators)**

4. **RSI (相對強弱指標)**
   - 調整週期：2~50
   - 選擇顏色
   - **測試範例**：週期 `9`，顏色改為 `紫色`

5. **MACD (指數平滑異同移動平均線)**
   - Fast (快線)：2~50
   - Slow (慢線)：5~100
   - Signal (訊號線)：2~50
   - **測試範例**：快線 `8`，慢線 `21`，訊號線 `5`

6. **KD (隨機指標)**
   - K 週期：2~50
   - D 週期：2~20
   - **測試範例**：K `14`，D `5`

7. **ADX/CCI/Williams %R**
   - 各自調整週期：2~50
   - **測試範例**：ADX `20`，CCI `20`，WR `10`

---

### 4️⃣ 應用變更

1. **確認按鈕**
   - 點擊 `Confirm` (確認) 按鈕
   - ✅ 控制台會輸出：`✓ 指標參數已應用到圖表`
   - ✅ 狀態列會顯示：`指標參數已更新`
   - ✅ **圖表會立即重新計算並更新顯示**

2. **取消按鈕**
   - 點擊 `Cancel` (取消) 按鈕
   - 不應用任何變更

3. **重設按鈕**
   - 點擊 `Reset` (重設) 按鈕
   - 所有參數恢復到預設值

---

## 🔍 驗證效果

### ✅ **預期結果**

1. **SMA/EMA 週期變化**
   - 週期變小 → 線條更貼近 K 線，波動更靈敏
   - 週期變大 → 線條更平滑，反應較慢

   **實例**：
   - SMA(10) 會比 SMA(20) 更快速反應價格變化
   - EMA(50) 會比 EMA(20) 更平滑

2. **顏色變化**
   - 圖表中的指標線條顏色會立即改變
   - SMA 改為紅色 → 圖表上的 SMA 線會變紅

3. **標題更新**
   - 圖表標題會反映新參數
   - 例如：`SMA(10)` 取代原本的 `SMA(20)`

4. **RSI/MACD 等副圖指標**
   - 參數改變後，副圖會重新計算並更新

5. **BOLL 倍數調整**
   - 倍數變小 → 上下軌道縮小，更靠近中軌
   - 倍數變大 → 通道寬度增加

---

## 🐛 預期行為

### ✓ **正常情況**

1. ✅ 對話框打開時，顯示當前圖表中的參數
2. ✅ 修改後點擊確認，圖表立即更新
3. ✅ 切換指標（例如從 SMA 切換到 EMA），新參數生效
4. ✅ 重新打開對話框，顯示最後一次設定的值

### ⚠️ **已知限制**

1. **K 線數量不足**
   - 如果 SMA(50) 但圖表只有 30 根 K 線
   - 指標不會顯示（需要 50 根 K 線）
   - **解決方案**：匯入更多歷史資料或等待實時資料累積

2. **指標未啟用**
   - 如果工具列的指標下拉未選擇該指標
   - 即使設定了參數也不會顯示
   - **解決方案**：從工具列選擇要顯示的指標

3. **顏色按鈕限制**
   - 目前只有 SMA、EMA、RSI 支援顏色選擇
   - MACD、KD 等多線指標的顏色需要在後續版本中完善

---

## 🎨 實際測試案例

### 案例 1：短線交易者設定

```
SMA:  10 (紅色)    - 短期趨勢
EMA:  5  (黃色)    - 極短期訊號
RSI:  9  (橙色)    - 快速超買超賣
MACD: 8/21/5       - 敏感 MACD
```

**預期效果**：更快速反應價格變化，適合短線操作

---

### 案例 2：長線投資者設定

```
SMA:  50 (藍色)    - 中期趨勢
EMA:  100 (綠色)   - 長期趨勢
RSI:  21 (紫色)    - 平穩 RSI
MACD: 12/26/9      - 標準 MACD
BOLL: 30, 2.5      - 寬鬆通道
```

**預期效果**：更平滑的指標線，過濾短期噪音

---

### 案例 3：極限測試

```
SMA:  200          - 非常長期
RSI:  2            - 極短 RSI (極端超買超賣)
BOLL: 20, 0.5      - 極窄通道
```

**預期行為**：
- SMA(200) 可能沒有足夠資料顯示
- RSI(2) 會非常敏感，頻繁觸及 0/100
- BOLL 通道會非常窄

---

## 📝 控制台輸出範例

```
指標設定已確認:
SMA Period: 10
EMA Period: 50
RSI Period: 9
MACD: 8/21/5
BOLL: 30, 1.5
KD: 14/5
✓ 指標參數已應用到圖表
```

---

## 🔧 技術細節

### 實作流程

1. **載入當前配置**
   ```java
   IndicatorConfig smaConfig = chartDock.getIndicatorConfig("SMA");
   smaPeriodSpinner.setValue(smaConfig.getPeriod());
   ```

2. **應用新配置**
   ```java
   IndicatorConfig newConfig = new IndicatorConfig();
   newConfig.setPeriod(dialog.getSmaPeriod());
   newConfig.setColor(dialog.getSmaColor());
   chartDock.setIndicatorParameters("SMA", newConfig);
   ```

3. **重新計算指標**
   - `setIndicatorParameters()` 內部會調用 `updateIndicators()`
   - 清除舊資料 → 重新計算 → 更新圖表 → 刷新畫面

---

## 🎯 測試檢查清單

- [ ] 打開對話框，顯示預設參數
- [ ] 修改 SMA 週期，確認後圖表更新
- [ ] 修改 SMA 顏色，確認後顏色改變
- [ ] 切換到 BOLL 指標，修改倍數，觀察通道寬度
- [ ] 修改 RSI 週期，觀察副圖變化
- [ ] 點擊「重設」按鈕，參數恢復預設
- [ ] 點擊「取消」按鈕，參數不變
- [ ] 重新打開對話框，確認顯示最後一次設定的值
- [ ] 狀態列顯示 "指標參數已更新"
- [ ] 圖表標題反映新參數（如 `SMA(10)`）

---

## ✨ 未來增強功能

1. **預設方案**
   - 儲存常用參數組合
   - 快速切換「短線」「中線」「長線」模式

2. **更多顏色選項**
   - 支援 MACD 三條線的顏色設定
   - KD 的 K 線和 D 線分別設色

3. **參數驗證**
   - MACD Fast < Slow 的檢查
   - 週期不能超過可用 K 線數量的警告

4. **即時預覽**
   - 在對話框中調整參數時，即時更新圖表（不需要點確認）

---

## 📊 總結

✅ **功能完整度：100%**

- ✅ 對話框 UI 完成
- ✅ 參數載入完成
- ✅ 參數應用完成
- ✅ 圖表更新完成
- ✅ 國際化完成
- ✅ 狀態反饋完成

**現在可以盡情測試並根據交易策略自訂指標參數了！** 🎉

