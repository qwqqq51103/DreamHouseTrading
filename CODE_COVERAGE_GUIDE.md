# 代碼覆蓋率指南

## 什麼是代碼覆蓋率？

代碼覆蓋率 (Code Coverage) 是一個衡量測試完整性的指標，顯示有多少程式碼被測試執行過。

### 覆蓋率類型

- **行覆蓋率 (Line Coverage)**: 有多少行代碼被執行
- **分支覆蓋率 (Branch Coverage)**: 有多少 if/else 分支被執行
- **方法覆蓋率 (Method Coverage)**: 有多少方法被調用

## 🚀 快速開始

### 方法 1: 使用批次文件（推薦）

```bash
# 執行測試並生成覆蓋率報告
run-coverage.bat
```

報告會自動在瀏覽器中打開。

### 方法 2: 使用 Maven 命令

```bash
# 清理並執行測試，生成覆蓋率報告
mvn clean test

# 只生成報告（不重新執行測試）
mvn jacoco:report
```

### 方法 3: 使用 IDE

**IntelliJ IDEA:**
1. 右鍵點擊測試類或測試目錄
2. 選擇 "Run Tests with Coverage"

**Eclipse:**
1. 安裝 EclEmma 插件（通常已內建）
2. 右鍵點擊專案 → Coverage As → JUnit Test

## 📊 查看報告

覆蓋率報告位置：`target/site/jacoco/index.html`

### 報告內容

報告會顯示：
- 📦 **套件級別覆蓋率**
- 📄 **類別級別覆蓋率**
- 🔢 **方法級別覆蓋率**
- 📝 **行級別覆蓋率（含源碼高亮）**

### 顏色含義

- 🟢 **綠色**: 已覆蓋
- 🟡 **黃色**: 部分覆蓋（分支）
- 🔴 **紅色**: 未覆蓋

## 🎯 覆蓋率目標

本專案的覆蓋率目標：

| 模組 | 目標覆蓋率 | 當前狀態 |
|------|-----------|---------|
| 核心模型類 (Trade, Position, Portfolio) | 100% | ✅ 100% |
| 業務邏輯類 (BacktestEngine, Strategies) | 80%+ | ⏳ 進行中 |
| 工具類 (AdvancedStopLossManager) | 90%+ | ✅ 90% |
| UI 類 | 30%+ | ❌ 0% |
| **整體目標** | **70%+** | ⏳ **~50%** |

## ⚙️ JaCoCo 配置說明

### pom.xml 配置

```xml
<plugin>
    <groupId>org.jacoco</groupId>
    <artifactId>jacoco-maven-plugin</artifactId>
    <version>0.8.11</version>
    <executions>
        <!-- 在測試前準備代理 -->
        <execution>
            <id>prepare-agent</id>
            <goals>
                <goal>prepare-agent</goal>
            </goals>
        </execution>

        <!-- 生成報告 -->
        <execution>
            <id>report</id>
            <phase>test</phase>
            <goals>
                <goal>report</goal>
            </goals>
        </execution>

        <!-- 檢查覆蓋率門檻 -->
        <execution>
            <id>check</id>
            <goals>
                <goal>check</goal>
            </goals>
            <configuration>
                <rules>
                    <rule>
                        <element>PACKAGE</element>
                        <limits>
                            <limit>
                                <counter>LINE</counter>
                                <value>COVEREDRATIO</value>
                                <minimum>0.30</minimum>  <!-- 最低 30% -->
                            </limit>
                        </limits>
                    </rule>
                </rules>
            </configuration>
        </execution>
    </executions>
</plugin>
```

### 配置參數說明

- **minimum**: 最低覆蓋率要求（0.30 = 30%）
- **counter**: 計數器類型（LINE, BRANCH, METHOD, CLASS）
- **value**: 值類型（COVEREDRATIO, MISSEDCOUNT）

## 📈 提升覆蓋率策略

### 1. 識別未覆蓋代碼

查看報告中的紅色部分，優先為以下代碼撰寫測試：
- 核心業務邏輯
- 複雜算法
- 錯誤處理分支
- 邊界條件

### 2. 撰寫針對性測試

```java
@Test
@DisplayName("測試邊界條件 - 價格為零")
void testBoundaryConditionZeroPrice() {
    // 針對未覆蓋的邊界條件撰寫測試
}
```

### 3. 提升分支覆蓋率

確保測試覆蓋所有 if/else 分支：

```java
// 原代碼
if (price > 0) {
    // 分支 A
} else {
    // 分支 B
}

// 測試應該覆蓋兩個分支
@Test void testPricePositive() { ... }  // 覆蓋分支 A
@Test void testPriceZero() { ... }      // 覆蓋分支 B
```

## 🔍 分析覆蓋率報告

### 範例報告解讀

```
Package: com.dreamhouse.trading.core.backtest
├─ Trade.java                    100% (45/45 lines)   ✅
├─ Position.java                 100% (120/120 lines) ✅
├─ Portfolio.java                100% (150/150 lines) ✅
├─ BacktestEngine.java            65% (180/277 lines) ⚠️
├─ SimpleMovingAverageStrategy    40% (60/150 lines)  ❌
└─ AdvancedStopLossManager        90% (200/222 lines) ✅
```

**優先改進**：
1. SimpleMovingAverageStrategy (40%) - 需要更多測試
2. BacktestEngine (65%) - 部分邏輯未測試

### 深入查看未覆蓋代碼

1. 點擊報告中的類別名稱
2. 查看源碼高亮
3. 找到紅色/黃色標記的行
4. 撰寫測試覆蓋這些行

## 🚫 排除不需要測試的代碼

某些代碼不需要測試覆蓋（如 getter/setter、toString）：

### 方法 1: 註解排除

```java
// 排除整個類
@Generated
public class AutoGeneratedClass { ... }

// 排除特定方法
@Generated
public String toString() { ... }
```

### 方法 2: pom.xml 排除

```xml
<configuration>
    <excludes>
        <exclude>**/*DTO.class</exclude>
        <exclude>**/*Config.class</exclude>
    </excludes>
</configuration>
```

## 📊 持續監控

### 在 CI/CD 中整合

```yaml
# GitHub Actions 範例
- name: Run tests with coverage
  run: mvn clean test

- name: Upload coverage to Codecov
  uses: codecov/codecov-action@v3
  with:
    files: ./target/site/jacoco/jacoco.xml
```

### 設置覆蓋率門檻

當覆蓋率低於目標時，建構失敗：

```xml
<minimum>0.70</minimum>  <!-- 要求 70% 覆蓋率 -->
```

## 💡 最佳實踐

### ✅ 好的做法

1. **定期檢查覆蓋率**：每次提交前查看報告
2. **增量改進**：新代碼必須有測試
3. **平衡覆蓋率與質量**：不要為了覆蓋率而寫無意義的測試
4. **關注核心業務邏輯**：優先測試關鍵功能

### ❌ 避免的做法

1. **盲目追求 100%**：某些代碼（如簡單 getter）不需測試
2. **只測簡單場景**：忽略異常和邊界條件
3. **為了覆蓋率而測試**：測試應該驗證行為，不只是執行代碼

## 🔗 相關資源

- [JaCoCo 官方文檔](https://www.jacoco.org/jacoco/trunk/doc/)
- [Maven JaCoCo Plugin](https://www.jacoco.org/jacoco/trunk/doc/maven.html)
- [測試最佳實踐](https://github.com/goldbergyoni/javascript-testing-best-practices)

## 🎯 下一步行動

1. ✅ 執行 `run-coverage.bat` 查看當前覆蓋率
2. ⏳ 識別覆蓋率低的模組
3. ⏳ 為核心業務邏輯撰寫測試
4. ⏳ 達到 70% 整體覆蓋率目標

---

**更新日期**: 2025-01-09
**工具版本**: JaCoCo 0.8.11
**配置狀態**: ✅ 已完成
